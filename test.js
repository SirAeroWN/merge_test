/**
 * A test function
 *
 * @param {number} a
 * @param {number} b
 *
 * @return {number}
 */
function test(a, b) {
    return a + b;
}

console.log(test(3, 4))
test(a,   b);
console.log(test(4, 5));
